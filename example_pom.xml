<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>{yourgroup}</groupId>
    <artifactId>{yourid}</artifactId>
    <version>{yourversion}</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <plugin.path>./libs/{original.jar}</plugin.path>
    </properties>

    <repositories>
        <repository>
            <id>turbojaxReleases</id>
            <url>https://maven.turbojax.org/releases</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>local</groupId>
            <artifactId>plugin</artifactId>
            <version>1</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/${plugin.path}</systemPath>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>${project.basedir}/src/main/java</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.turbojax</groupId>
                <artifactId>jarinjector</artifactId>
                <version>1</version>
                <configuration>
                    <pluginJar>${plugin.path}</pluginJar>
                </configuration>

                <executions>
                    <execution>
                        <phase>install</phase>
                        <goals>
                            <goal>inject</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
